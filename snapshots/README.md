# snapshots/

Vazio até o marco D2. Guarda a captura bruta (HTML) de cada fonte, para
auditoria e reprocessamento — mesma estrutura de caminho de `../dados/`,
com um arquivo por data de captura:
`br/<esfera>/<tipo>/<identificador>/<fonte>/AAAA-MM-DD.html`.
